handoff:
  from_agent: "po"
  to_agent: "dev"
  last_command: "validate-story-draft"
  story_context:
    story_id: "75-368"
    story_path: "docs/stories/75-368-follow-up-nicole-por-lead.story.md"
    story_status: "Ready"
    current_task: "T1..T6 — implementar"
    branch: "main (criar feature branch)"
  decisions:
    - "GO 10/10 pos-correcoes. Story Ready."
    - "AC2 e o coracao: condicao no if de followup/route.ts:360, NUNCA filtro na consulta da linha 203 (mataria o alert_broker)"
    - "AC5 rota fixada: POST /api/leads/[id]/followup-nicole, permissao espelhando resume-ai (can() OU corretor dono), idempotente"
    - "Rotulo do botao PRECISA conter 'Follow-up' — ja existe is_ai_active (Epic 63) e o operador nao pode confundir os dois"
    - "Migration 240. Coluna leads.nicole_followup_off_at timestamptz, NULL = ligado"
    - "Fora de escopo (5 itens) e proibicao de implementar extras — respeitar"
  files_modified:
    - "docs/stories/75-368-follow-up-nicole-por-lead.story.md (Draft -> Ready)"
    - "docs/backlog.md (item novo: cron ignora is_ai_active — aguarda decisao do Marcos, NAO implementar)"
  blockers: []
  next_action: "@dev *develop 75-368"
  consumed: false
