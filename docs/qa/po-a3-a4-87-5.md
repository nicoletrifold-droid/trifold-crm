# Parecer @po — A3 e A4 do gate da Story 87-5

**Data:** 2026-08-15 · **Autor:** @po (Pax) · **Gate:** `docs/qa/gates/87.5-historico-rotulado-fala-do-corretor.yml` (round 2, CONCERNS)
**Escopo:** só as seções de AC12 e de rollback da story. Nenhuma linha de código, nenhum epic tocado.
**Veredito:** **A3 FECHADO · A4 FECHADO por declaração** (não há gatilho automático; substituto nomeado).
**Um item NÃO fechável por mim, nomeado:** o responsável da AC12 continua indefinido — dono **@pm/Gabriel**.

---

## O denominador que ninguém tinha declarado

Tudo abaixo depende disto, e foi medido por mim contra produção (`dsopqkqjkmhytudaaolv`) em 15/08.

A população que esta story muda **não** são "as respostas da Nicole". É o **turno da Nicole cujo
histórico já continha fala de corretor na mesma conversa**:

| medida (30 dias) | valor |
|---|---|
| turnos da Nicole (`role='assistant'`) | **588** |
| dos quais na população-alvo | **34** (13 conversas) = **1,13/dia** |
| distribuição | **em rajada:** 9 dos últimos 15 dias em **ZERO**; um dia com 6 |
| P(janela de 24 h com zero turno observável) | **~60 %** |

---

## A3 — a AC12-(ii) foi reescrita, mas não para a métrica que o gate propôs

Rodei as duas réguas candidatas **antes** de adotar qualquer uma.

**A régua anterior ("valor do corretor repetido", busca textual) está saturada em zero:** 0 de 34.
O único turno da população com cifra (05/08, *"seu orçamento de R$ 400 mil"*) repete o orçamento **do
próprio lead**. 0 antes, 0 depois — não distingue mundo nenhum.

**A régua que o gate mandou adotar ("regra comercial interna: entrada mínima 20 % / 80 mil") é pior,
e por três motivos medidos:**

1. **Ela não é interna.** O prompt manda dizer, literalmente:
   `packages/ai/src/prompts/property-presentation.ts:48-52` — *"A entrada fica em torno de 80 mil
   reais"* — e `qualification.ts:34-36`. Contar essa frase é contar a Nicole **obedecendo**. Não está
   no `personality_prompt` do banco, nem no `knowledge_base`, nem em `properties`: está no código, como
   instrução prescrita.
2. **Na população-alvo ela vale 0 de 34** hoje.
3. **As 4 ocorrências dos últimos 30 dias estão todas na população oposta** — turnos **anteriores** a
   qualquer fala de corretor (07/08 ×2, 12/08, e a de 28/07 que ecoa o *"dando 80k de entrada"* dito
   pelo **próprio lead**). A story **não muda nenhuma das quatro**. **Precisão para o defeito-alvo:
   0 de 4.**

O `7/10 → 0/10` do experimento é real — mas é **condicional à fixture**, que é o pior caso construído.
Em produção esse denominador vale 34 turnos por mês, e neles a frase não aparece.

**O que a AC12-(ii) passou a ser:** classificação em três saídas nomeadas — **ATRIBUI · CONTRADIZ ·
DESMENTE** — fechada pelo **turno provocado** da AC12-(i); leitura observacional de **todos** os
turnos da população (consulta SQL colada na AC), com **piso: n < 5 ⇒ INCONCLUSIVO, jamais "sem
regressão"**, extensão até n ≥ 5 ou 7 dias corridos, e **controle positivo provocado** (conversa sem
corretor em que a Nicole **deve continuar** falando de entrada — emudecer também é reversão).

---

## A4 — não há gatilho automático. Declarado, com quem/o quê/quando

1. **O gatilho como estava escrito é estruturalmente inalcançável.** `stripSystemBlocks` roda antes de
   persistir e de enviar (`pipeline.ts:1181-1186`) e `saveMessages` grava o texto **já limpo**
   (`:1625`). `messages` com `[CORRETOR HUMANO` = **0**, e continuará 0 haja o que houver.
2. **O evento que sobra nunca disparou — nenhuma vez, em toda a história.**
   `NICOLE_SYSTEM_BLOCK_LEAK` = **0 registros** em `system_events`, contra **471** `CLAUDE_RESPONSE`
   em 30 dias (o caminho de emissão funciona). E **o único vazamento real de marcação em produção não
   gerou evento**: a mensagem `3a885e0e-bc31-4f8a-a0f6-13fc159bde38`, de **06/08 10:04 UTC**, foi
   entregue ao lead com `[SISTEMA: horário 11h do sábado 08/08 — LIVRE]` — **~6 h antes** do deploy da
   75-279 (`fa100677`, 06/08 13:05 UTC). Não é falha da guarda; é **base rate**: **1 em 1.320** turnos
   em toda a história, com a exposição do `[SISTEMA`, que é muito maior que a do rótulo do corretor.
   Valor esperado em 24 h ≈ **0,02**. **"Zero ocorrências" é o resultado esperado inclusive num mundo
   quebrado.**
3. **Distinguir não exige campo novo — e o `marcacao:` fica fora desta fase** (caminho de decisão
   novo). O `metadata.raw_message` já guarda os **500 primeiros caracteres da saída crua**. Consulta
   que **rodei contra produção** (0 linhas, como previsto):

   ```sql
   select id, created_at, metadata->>'lead_id', metadata->>'raw_message'
   from system_events
   where event_type = 'NICOLE_SYSTEM_BLOCK_LEAK'
     and metadata->>'raw_message' ilike '%[CORRETOR HUMANO%';
   ```

   **Limite declarado:** marcação além do caractere 500 vira "indeterminada" e vai para inspeção
   manual.

**Substituto (é isto o gatilho):** o **responsável nomeado da AC12** roda a consulta acima ao fim da
janela (qualquer linha = reversão imediata; zero linhas **não** é evidência de sucesso), lê **um a um**
os turnos da população da AC12-(ii-b), e confere a **reapresentação** contra o baseline medido —
**3 em 40 turnos da população-alvo em 90 dias (7,5 %)**. A reapresentação virou **gatilho de inspeção,
não de reversão automática**: já ocorre hoje, e reverter na primeira ocorrência seria reverter por
defeito **herdado**.

---

## O que eu não consigo fechar, e de quem é

**O responsável nomeado da AC12 (Marcos ou Thielly) continua indefinido.** Por D7, sem nome **o deploy
não sai** — e nem o gate nem esta reescrita suprem o nome. **Dono: @pm/Gabriel.** É o único item que
sobra entre a janela e um resultado legível.
